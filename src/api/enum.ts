export enum EApi {
  AUTH = 'AUTH',
  FOLDER = 'FOLDER',
  SPECIMEN = 'SPECIMEN',
}

export enum EApiAuth {
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  CHECK = 'CHECK',
}

export enum EApiBase {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  LIST = 'LIST',
  DELETE = 'DELETE',
}
