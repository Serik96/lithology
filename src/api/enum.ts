export enum EApi {
  AUTH = 'AUTH',
  FOLDER = 'FOLDER',
}

export enum EApiAuth {
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  CHECK = 'CHECK',
}

export enum EApiFolder {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  LIST = 'LIST',
  DELETE = 'DELETE',
}
